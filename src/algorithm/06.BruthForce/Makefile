# Makefile - General Makefile for build
#

# include gerneral Makefile configuration file
include ../Makefile.inc

# execute file list
EXE = combination boggle picnic boardcover clocksync

all : ${EXE}

clean : 
	${RM} *.o ${EXE}

showall :
	@ echo ${EXE}

${EXE} :
